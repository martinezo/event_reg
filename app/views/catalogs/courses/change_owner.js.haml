:plain
  $('#change-owner').html("#{escape_javascript(render 'change_owner')}");
  $('#change-owner').dialog({
    title: "#{@catalogs_course.name}",
    width: 450,
    modal: true,
  });
  load_jquery_ui();
